#include <stdlib.h>
#include <stdio.h>

void Soma()
{
   int num = 0, i = 1;
   printf("Digite um numero: ");
   scanf("%i", &num);

   while (i < num)
   {
       printf("%i + 1 = %i\n", i, i +1);
       i++;
   }
}
